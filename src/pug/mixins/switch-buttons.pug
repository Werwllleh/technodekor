

mixin switch-buttons(data)
	//brands
	.switch-buttons&attributes(attributes)
		.switch-buttons__body
			button.switch-buttons__button.active(data-view="default--view")
				+svg("view_1")
			button.switch-buttons__button(data-view="row--view")
				+svg("view_2")
